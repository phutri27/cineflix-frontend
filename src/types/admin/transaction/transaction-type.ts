export interface CinemaRevenueProp {
    cinemaName: string
    totalRevenue: number
}

export interface MovieRevenueProp {
    movieTitle: string
    totalRevenue: number
}

export interface UserRevenueProp {
    email: string
    totalSpent: number
}